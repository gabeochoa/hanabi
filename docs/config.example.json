{
  "_comment": "Copy this to ~/.config/hanabi/config.json (or point HANABI_CONFIG at it) and fill in your values. With this file present you do NOT need any HANABI_* env vars. Env vars, if set, override the file. Delete the file (or set backend to \"mock\") to use the zero-config offline sample data.",

  "backend": "http",
  "api_base_url": "https://YOUR-HOST/api/v1",
  "token": "YOUR_BEARER_TOKEN",

  "sessions_path": "/sessions",
  "messages_path": "/sessions/{id}/messages",

  "_stream_note": "OPTIONAL live token-by-token replies (Phase STREAM). Leave stream_path empty (or omit it) to disable http streaming — the app then uses the synchronous send path. When set (and backend is http), a reply is streamed over server-sent events: the adapter POSTs to stream_path and reads text/event-stream 'data: {json}' frames, mapping each frame's fields via the names below. All values are GENERIC placeholders; nothing about any real service is baked in. The offline mock streams with NO config at all.",
  "stream_path": "/sessions/{id}/stream",
  "field_event_type": "type",
  "field_event_text": "text",
  "field_event_title": "title",
  "event_type_text": "text",
  "event_type_thinking": "thinking",
  "event_type_tool_call": "tool_call",
  "event_type_done": "done",
  "event_type_title_update": "title_update",

  "_field_note": "Field-name overrides map the adapter onto your backend's JSON shape. The defaults below match a sessions:[] + messages-with-blocks[] layout; change only if your backend differs.",
  "field_updated_at": "updatedAt",
  "field_created_at": "createdAt",
  "field_id": "id",
  "field_title": "title",
  "field_status": "status",
  "field_messages": "messages",
  "field_role": "role",
  "field_blocks": "blocks",
  "field_block_type": "type",
  "field_block_content": "content",
  "field_block_text_type": "text",

  "_auth_note": "OPTIONAL in-app device-code login (Phase AUTH). When backend is http, base_url is set, AND no token is stored (neither the 'token' key above nor a persisted ~/.config/hanabi/token.json), hanabi runs the device-code flow: it mints its own device code (a UUIDv4), POSTs it to auth_device_path to get a short user code + an authUrl, shows you the code + URL, polls auth_token_path every auth_poll_interval seconds until you approve in a browser, then persists the bearer token to ~/.config/hanabi/token.json (mode 0600) so you stay signed in. Provide the 'token' above (or run the offline mock) to skip the flow entirely. The paths below default to the generic navi-CLI auth paths; they are SIBLINGS of the API (NOT under base_url's path prefix), so the flow uses base_url's scheme+host ORIGIN by default — set auth_base_url to override. No client_id/secret exists in this flow. Nothing about any real host is baked into hanabi.",
  "auth_device_path": "/api/cli/auth/code",
  "auth_token_path": "/api/cli/auth/poll",
  "auth_base_url": "",
  "auth_client_type": "cli",
  "auth_poll_interval": 2,
  "auth_expires_in": 600,
  "field_device_code": "deviceCode",
  "field_client_type": "clientType",
  "field_poll_query": "code",
  "field_user_code": "userCode",
  "field_auth_url": "authUrl",
  "field_auth_status": "status",
  "field_token": "token",
  "auth_status_pending": "pending",
  "auth_status_authorized": "authorized"
}
